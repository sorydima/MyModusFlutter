# 🧪 Testing MyModusFlutter

## Unit Tests
- Located in the `test/` directory
- Run using: `flutter test`

## Widget Tests
- Use `WidgetTester` to verify UI components

## Integration Tests (Planned)
- Automate user flows with `integration_test` package