import 'dart:io';
import 'package:test/test.dart';
// This is a scaffold for integration tests. To run integration tests, start the server and a test database.
void main() {
  test('placeholder', () {
    expect(1, equals(1));
  });
}
