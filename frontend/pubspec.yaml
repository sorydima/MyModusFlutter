name: mymodus_frontend
environment:
  sdk: '>=2.17.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

dependencies:
  cached_network_image: ^3.2.3
  shimmer: ^2.0.0
  animations: ^2.0.6
  flutter_staggered_grid_view: ^0.6.2
